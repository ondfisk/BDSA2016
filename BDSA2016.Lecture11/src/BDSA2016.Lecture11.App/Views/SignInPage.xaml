<Page
    x:Class="BDSA2016.Lecture11.App.Views.SignInPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BDSA2016.Lecture11.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Command="{Binding SignInOrOutCommand}" FontSize="30" Content="{Binding Button, Mode=OneWay}" Margin="25" />
            <TextBlock FontSize="30" Foreground="Green" Text="{Binding Label, Mode=OneWay}" Margin="25,0,25,25" />
            <Button Command="{Binding BackCommand}" Content="Home" Margin="25,50,25,25" />
        </StackPanel>
    </Grid>
</Page>
